from pathlib import Path
import re
from datetime import datetime

ROOT = Path.cwd()
HTML_PATH = ROOT / "blog" / "templates" / "blog" / "settings" / "_settings_tab.html"
JS_PATH = ROOT / "blog" / "static" / "blog" / "js" / "blog_settings_avatar.js"
STAMP = datetime.now().strftime("%Y%m%d_%H%M%S")

if not HTML_PATH.exists():
    raise SystemExit(f"Ne postoji file: {HTML_PATH}")
if not JS_PATH.exists():
    raise SystemExit(f"Ne postoji file: {JS_PATH}")

html_backup = HTML_PATH.with_suffix(HTML_PATH.suffix + f".bak_avatar_clean_final_{STAMP}")
js_backup = JS_PATH.with_suffix(JS_PATH.suffix + f".bak_avatar_clean_final_{STAMP}")
html_backup.write_text(HTML_PATH.read_text(encoding="utf-8"), encoding="utf-8")
js_backup.write_text(JS_PATH.read_text(encoding="utf-8"), encoding="utf-8")

html = HTML_PATH.read_text(encoding="utf-8")
replacements = {
    "Okrugli izrez kao na Gmailu, s pomicanjem, zumom i rotacijom.": "Uredite avatar prije spremanja.",
    "Odaberi sliku, pomakni kadar, zumiraj i spremi okrugli izrez.": "Odaberite sliku i namjestite prikaz avatara.",
    "Povuci sliku, zumiraj i spremi okrugli izrez.": "Namjestite sliku unutar okvira.",
    ">Zum<": ">Uvećanje<",
    ">Reset<": ">Vrati<",
}
for old, new in replacements.items():
    html = html.replace(old, new)

html = re.sub(r"
?<style>\s*/\* Avatar final clean editor \*/.*?</style>\s*", "
", html, flags=re.DOTALL)
css = '<style>\n/* Avatar final clean editor */\n#avatarCropModal .modal-dialog { max-width: 760px !important; }\n#avatarCropModal .modal-content,\n#avatarCropModal .avatar-crop-modal {\n    border-radius: 22px !important;\n    padding: 20px 24px 18px !important;\n    max-height: calc(100vh - 38px) !important;\n    overflow: hidden !important;\n}\n#avatarCropModal .modal-header { padding: 0 0 8px 0 !important; min-height: 0 !important; }\n#avatarCropModal .modal-title,\n#avatarCropModal h1,\n#avatarCropModal h2,\n#avatarCropModal h3 {\n    font-size: 24px !important;\n    line-height: 1.15 !important;\n    margin: 0 0 4px 0 !important;\n    font-weight: 700 !important;\n}\n#avatarCropModal p,\n#avatarCropModal .text-muted,\n#avatarCropModal .small {\n    font-size: 14px !important;\n    line-height: 1.25 !important;\n    margin-bottom: 10px !important;\n}\n#avatarCropModal .btn-close,\n#avatarCropModal [data-bs-dismiss="modal"] {\n    width: 28px !important;\n    height: 28px !important;\n    min-width: 28px !important;\n    min-height: 28px !important;\n    padding: 0 !important;\n    font-size: 18px !important;\n    line-height: 1 !important;\n    opacity: .9 !important;\n}\n#avatarCropModal .modal-body { padding: 8px 0 0 0 !important; }\n#avatarCropModal .avatar-crop-stage,\n#avatarCropModal .avatar-crop-frame {\n    position: relative !important;\n    width: min(100%, 640px) !important;\n    height: 300px !important;\n    margin: 0 auto !important;\n    overflow: hidden !important;\n    background: #05080d !important;\n    border-radius: 14px !important;\n}\n#avatarCropModal .avatar-crop-frame::after,\n#avatarCropModal .avatar-crop-stage::after {\n    content: "";\n    position: absolute;\n    left: 50%;\n    top: 50%;\n    width: var(--avatar-circle-size, 190px);\n    height: var(--avatar-circle-size, 190px);\n    transform: translate(-50%, -50%);\n    border: 3px solid rgba(255,255,255,.96);\n    border-radius: 50%;\n    box-shadow: 0 0 0 9999px rgba(0,0,0,.30);\n    pointer-events: none;\n    z-index: 3;\n}\n#avatarCropImage {\n    position: absolute !important;\n    left: 50% !important;\n    top: 50% !important;\n    max-width: none !important;\n    max-height: none !important;\n    width: auto !important;\n    height: auto !important;\n    transform-origin: center center !important;\n    user-select: none !important;\n    -webkit-user-drag: none !important;\n    cursor: grab !important;\n    z-index: 2 !important;\n    border-radius: 0 !important;\n}\n#avatarCropImage.is-dragging { cursor: grabbing !important; }\n#avatarCropModal .cropper-container,\n#avatarCropModal .cropper-wrap-box,\n#avatarCropModal .cropper-canvas,\n#avatarCropModal .cropper-drag-box,\n#avatarCropModal .cropper-crop-box { display: none !important; }\n#avatarCropModal .avatar-crop-controls,\n#avatarCropModal .avatar-controls,\n#avatarCropModal .modal-footer {\n    width: min(100%, 640px) !important;\n    margin: 12px auto 0 !important;\n    padding: 0 !important;\n    display: flex !important;\n    align-items: center !important;\n    gap: 10px !important;\n    flex-wrap: wrap !important;\n}\n#avatarCropModal label,\n#avatarCropModal .form-label {\n    font-size: 15px !important;\n    line-height: 1.2 !important;\n    margin: 0 !important;\n    font-weight: 600 !important;\n}\n#avatarZoomRange {\n    flex: 1 1 280px !important;\n    height: 6px !important;\n    margin: 0 !important;\n}\n#avatarCropModal .btn,\n#avatarCropModal button {\n    font-size: 14px !important;\n    line-height: 1.2 !important;\n    padding: 7px 12px !important;\n    border-radius: 8px !important;\n    min-height: 34px !important;\n}\n#avatarApplyBtn {\n    margin-left: auto !important;\n    font-size: 15px !important;\n    padding: 8px 18px !important;\n    min-width: 150px !important;\n}\n@media (max-width: 768px) {\n    #avatarCropModal .modal-dialog { max-width: calc(100vw - 18px) !important; }\n    #avatarCropModal .modal-content,\n    #avatarCropModal .avatar-crop-modal { padding: 16px !important; }\n    #avatarCropModal .avatar-crop-stage,\n    #avatarCropModal .avatar-crop-frame { height: 260px !important; }\n    #avatarApplyBtn { margin-left: 0 !important; width: 100% !important; }\n}\n</style>'
html = html.rstrip() + "
" + css + "
"
HTML_PATH.write_text(html, encoding="utf-8")

js = 'document.addEventListener("DOMContentLoaded", function () {\n    const form = document.getElementById("avatarForm");\n    const input = document.getElementById("avatarInput");\n    const triggerBtn = document.getElementById("avatarChangeBtn");\n    const hiddenInput = document.getElementById("croppedImage");\n    const currentAvatarImage = document.getElementById("currentAvatarImage");\n    const previewCanvas = document.getElementById("avatarPreviewCanvas");\n    const previewMiniCanvas = document.getElementById("avatarPreviewMiniCanvas");\n    const modalEl = document.getElementById("avatarCropModal");\n    const cropImage = document.getElementById("avatarCropImage");\n    const zoomRange = document.getElementById("avatarZoomRange");\n    const rotateBtn = document.getElementById("avatarRotateBtn");\n    const resetBtn = document.getElementById("avatarResetBtn");\n    const applyBtn = document.getElementById("avatarApplyBtn");\n\n    if (!form || !input || !hiddenInput || !previewCanvas || !previewMiniCanvas || !cropImage) return;\n\n    let avatarModal = null;\n    if (modalEl && window.bootstrap) avatarModal = new bootstrap.Modal(modalEl);\n\n    let imageLoaded = false;\n    let imageObjectUrl = null;\n    let stage = null;\n    let naturalWidth = 0;\n    let naturalHeight = 0;\n    let baseScale = 1;\n    let zoomFactor = 1;\n    let offsetX = 0;\n    let offsetY = 0;\n    let rotation = 0;\n    let circleSize = 190;\n    let dragging = false;\n    let dragStartX = 0;\n    let dragStartY = 0;\n    let startOffsetX = 0;\n    let startOffsetY = 0;\n\n    function getStage() {\n        if (stage) return stage;\n        stage = cropImage.closest(".avatar-crop-frame") || cropImage.closest(".avatar-crop-stage") || cropImage.parentElement;\n        return stage;\n    }\n\n    function clearCanvas(targetCanvas) {\n        if (!targetCanvas) return;\n        const ctx = targetCanvas.getContext("2d");\n        ctx.clearRect(0, 0, targetCanvas.width, targetCanvas.height);\n    }\n\n    function drawCircularImage(source, targetCanvas) {\n        if (!source || !targetCanvas) return;\n        const ctx = targetCanvas.getContext("2d");\n        const width = targetCanvas.width;\n        const height = targetCanvas.height;\n        ctx.clearRect(0, 0, width, height);\n        ctx.save();\n        ctx.beginPath();\n        ctx.arc(width / 2, height / 2, Math.min(width, height) / 2, 0, Math.PI * 2);\n        ctx.closePath();\n        ctx.clip();\n        ctx.drawImage(source, 0, 0, width, height);\n        ctx.restore();\n    }\n\n    function renderImagePreview(imageLike) {\n        if (!imageLike) {\n            clearCanvas(previewCanvas);\n            clearCanvas(previewMiniCanvas);\n            return;\n        }\n        drawCircularImage(imageLike, previewCanvas);\n        drawCircularImage(imageLike, previewMiniCanvas);\n    }\n\n    function normalizedRotation() { return ((rotation % 360) + 360) % 360; }\n\n    function rotatedSize() {\n        const rot = normalizedRotation();\n        if (rot === 90 || rot === 270) return { width: naturalHeight, height: naturalWidth };\n        return { width: naturalWidth, height: naturalHeight };\n    }\n\n    function calculateBaseScale() {\n        const st = getStage();\n        if (!st || !naturalWidth || !naturalHeight) return 1;\n        const rect = st.getBoundingClientRect();\n        const rotated = rotatedSize();\n        return Math.min(rect.width / rotated.width, rect.height / rotated.height);\n    }\n\n    function calculateCircleSize() {\n        const st = getStage();\n        if (!st) return 180;\n        const rect = st.getBoundingClientRect();\n        const rotated = rotatedSize();\n        const displayedW = rotated.width * baseScale;\n        const displayedH = rotated.height * baseScale;\n        const minDisplayed = Math.min(displayedW, displayedH);\n        const byImage = minDisplayed * 0.68;\n        const byStage = Math.min(rect.width, rect.height) * 0.70;\n        return Math.max(120, Math.min(byImage, byStage, 210));\n    }\n\n    function clampOffsets() {\n        const rotated = rotatedSize();\n        const displayedW = rotated.width * baseScale * zoomFactor;\n        const displayedH = rotated.height * baseScale * zoomFactor;\n        const maxX = Math.max(0, (displayedW - circleSize) / 2);\n        const maxY = Math.max(0, (displayedH - circleSize) / 2);\n        offsetX = Math.min(maxX, Math.max(-maxX, offsetX));\n        offsetY = Math.min(maxY, Math.max(-maxY, offsetY));\n    }\n\n    function applyTransform() {\n        const st = getStage();\n        if (!st || !imageLoaded) return;\n        clampOffsets();\n        st.style.setProperty("--avatar-circle-size", `${circleSize}px`);\n        const scale = baseScale * zoomFactor;\n        cropImage.style.transform = `translate(-50%, -50%) translate(${offsetX}px, ${offsetY}px) rotate(${rotation}deg) scale(${scale})`;\n    }\n\n    function resetEditor() {\n        if (!cropImage.complete || !cropImage.naturalWidth) return;\n        naturalWidth = cropImage.naturalWidth;\n        naturalHeight = cropImage.naturalHeight;\n        rotation = 0;\n        offsetX = 0;\n        offsetY = 0;\n        zoomFactor = 1;\n        baseScale = calculateBaseScale();\n        circleSize = calculateCircleSize();\n        if (zoomRange) {\n            zoomRange.min = "100";\n            zoomRange.max = "300";\n            zoomRange.step = "1";\n            zoomRange.value = "100";\n        }\n        imageLoaded = true;\n        applyTransform();\n    }\n\n    function makeCroppedCanvas(size = 400) {\n        if (!imageLoaded || !naturalWidth || !naturalHeight) return null;\n        const canvas = document.createElement("canvas");\n        canvas.width = size;\n        canvas.height = size;\n        const ctx = canvas.getContext("2d");\n        ctx.fillStyle = "#ffffff";\n        ctx.fillRect(0, 0, size, size);\n        const factor = size / circleSize;\n        const scale = baseScale * zoomFactor;\n        ctx.save();\n        ctx.scale(factor, factor);\n        ctx.translate(circleSize / 2 + offsetX, circleSize / 2 + offsetY);\n        ctx.rotate((rotation * Math.PI) / 180);\n        ctx.scale(scale, scale);\n        ctx.drawImage(cropImage, -naturalWidth / 2, -naturalHeight / 2, naturalWidth, naturalHeight);\n        ctx.restore();\n        return canvas;\n    }\n\n    function updatePreviewFromEditor() {\n        const canvas = makeCroppedCanvas(320);\n        if (canvas) renderImagePreview(canvas);\n    }\n\n    function applyCropToHiddenInput() {\n        const canvas = makeCroppedCanvas(400);\n        if (!canvas) return false;\n        hiddenInput.value = canvas.toDataURL("image/jpeg", 0.92);\n        renderImagePreview(canvas);\n        return true;\n    }\n\n    function openEditorFromFile(file) {\n        if (!file || !file.type || !file.type.startsWith("image/")) return;\n        if (imageObjectUrl) URL.revokeObjectURL(imageObjectUrl);\n        imageObjectUrl = URL.createObjectURL(file);\n        hiddenInput.value = "";\n        imageLoaded = false;\n        cropImage.classList.remove("is-dragging");\n        cropImage.removeAttribute("style");\n        cropImage.src = imageObjectUrl;\n        if (avatarModal) avatarModal.show();\n    }\n\n    if (currentAvatarImage) {\n        if (currentAvatarImage.complete) renderImagePreview(currentAvatarImage);\n        else currentAvatarImage.addEventListener("load", function () { renderImagePreview(currentAvatarImage); }, { once: true });\n    }\n\n    if (triggerBtn) triggerBtn.addEventListener("click", function () { input.click(); });\n\n    input.addEventListener("change", function (event) {\n        const file = event.target.files && event.target.files[0];\n        if (!file) return;\n        openEditorFromFile(file);\n    });\n\n    cropImage.addEventListener("load", function () {\n        requestAnimationFrame(function () { resetEditor(); updatePreviewFromEditor(); });\n    });\n\n    if (modalEl) {\n        modalEl.addEventListener("shown.bs.modal", function () {\n            if (cropImage.complete && cropImage.naturalWidth > 0) { resetEditor(); updatePreviewFromEditor(); }\n        });\n        modalEl.addEventListener("hidden.bs.modal", function () {\n            dragging = false;\n            cropImage.classList.remove("is-dragging");\n        });\n    }\n\n    if (zoomRange) {\n        zoomRange.addEventListener("input", function () {\n            const value = Number(zoomRange.value || 100);\n            zoomFactor = Math.max(1, value / 100);\n            applyTransform();\n            updatePreviewFromEditor();\n        });\n    }\n\n    cropImage.addEventListener("pointerdown", function (event) {\n        if (!imageLoaded) return;\n        dragging = true;\n        dragStartX = event.clientX;\n        dragStartY = event.clientY;\n        startOffsetX = offsetX;\n        startOffsetY = offsetY;\n        cropImage.classList.add("is-dragging");\n        cropImage.setPointerCapture(event.pointerId);\n    });\n\n    cropImage.addEventListener("pointermove", function (event) {\n        if (!dragging) return;\n        offsetX = startOffsetX + (event.clientX - dragStartX);\n        offsetY = startOffsetY + (event.clientY - dragStartY);\n        applyTransform();\n        updatePreviewFromEditor();\n    });\n\n    cropImage.addEventListener("pointerup", function (event) {\n        dragging = false;\n        cropImage.classList.remove("is-dragging");\n        try { cropImage.releasePointerCapture(event.pointerId); } catch (error) {}\n    });\n\n    cropImage.addEventListener("pointercancel", function () {\n        dragging = false;\n        cropImage.classList.remove("is-dragging");\n    });\n\n    if (rotateBtn) {\n        rotateBtn.addEventListener("click", function () {\n            rotation = (rotation + 90) % 360;\n            offsetX = 0;\n            offsetY = 0;\n            baseScale = calculateBaseScale();\n            circleSize = calculateCircleSize();\n            applyTransform();\n            updatePreviewFromEditor();\n        });\n    }\n\n    if (resetBtn) resetBtn.addEventListener("click", function () { resetEditor(); updatePreviewFromEditor(); });\n\n    if (applyBtn) {\n        applyBtn.addEventListener("click", function () {\n            const applied = applyCropToHiddenInput();\n            if (applied && avatarModal) avatarModal.hide();\n        });\n    }\n\n    form.addEventListener("submit", function (event) {\n        const submitter = event.submitter;\n        if (submitter && submitter.name === "delete_avatar") { hiddenInput.value = ""; return; }\n        if (input.files && input.files.length > 0 && !hiddenInput.value) applyCropToHiddenInput();\n    });\n\n    window.addEventListener("beforeunload", function () {\n        if (imageObjectUrl) URL.revokeObjectURL(imageObjectUrl);\n    });\n});\n'
JS_PATH.write_text(js, encoding="utf-8")

print("Gotovo: avatar editor je zamijenjen čistom verzijom.")
print(f"Backup HTML: {html_backup}")
print(f"Backup JS:   {js_backup}")
